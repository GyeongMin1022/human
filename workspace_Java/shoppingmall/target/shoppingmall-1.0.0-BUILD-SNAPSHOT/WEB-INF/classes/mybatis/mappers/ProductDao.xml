<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.shoppingmall.dao.ProductDao">

    <insert id="insertProduct" parameterType="kr.co.shoppingmall.dto.ProductDTO">
        INSERT INTO product1 (name, price, description)
        VALUES (#{name}, #{price}, #{description})
    </insert>

    <select id="findProductById" parameterType="long" resultType="kr.co.shoppingmall.dto.ProductDTO">
        SELECT id, name, price, description
        FROM product1
        WHERE id = #{id}
    </select>

    <select id="findAllProducts" resultType="kr.co.shoppingmall.dto.ProductDTO">
        SELECT id, name, price, description
        FROM product1
    </select>
</mapper>
